# The local development / CI environment: everything at once, because a single
# venv has to run the app, dbt, the listener and the whole test suite.
#
# The Docker images each install a strict subset instead: shipping dbt,
# Great Expectations and pytest inside the Streamlit image would drag in whole
# dependency trees it never imports. When a pin below changes, change it in the
# matching Dockerfile too.
-r requirements.txt
-r requirements-dbt.txt

# Streamlit app — mirrors docker/streamlit/Dockerfile
streamlit~=1.40
altair~=5.4

# MinIO event listener — mirrors docker/listener/Dockerfile
fastapi~=0.115
uvicorn[standard]~=0.32

# Data quality — mirrors docker/airflow/Dockerfile
great_expectations~=1.3

# Testing and linting
pytest~=8.3
pytest-cov~=6.0
httpx~=0.27
sqlfluff~=3.2
sqlfluff-templater-dbt~=3.2
ruff~=0.7
